```bash
poetry run semantic-release generate-config --pyproject >> pyproject.toml
```

```bash
poetry run semantic-release version
poetry run semantic-release version --patch
poetry run semantic-release version --vcs-release
```
